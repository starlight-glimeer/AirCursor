// AI 生成壁纸 —— 提示词、机器闸门、回喂循环。
//
// ⚠️⚠️⚠️ **这个功能的成败不在"能不能调 API"（那是 30 行），在"生成完怎么收敛"。**
//
// 用户 2026-08-02：「你说做不出来，纯属跟我扯淡呢，这个东西你拆成不烂，
//   找一下性啥的写好提示词设计一套 agent 的那种流程肯定能做出来的呀」
//
// **他说得对，而我原来那句话框错了问题** —— 我说的是"一次性生成做不到"，
// 而他问的是"拆开、设计流程能不能做到"。那是两件事。
// 而他抓的那个点正是关键：**粒子效果不需要素材，它就是代码**
// ⟹ 产物完全在模型能力范围内，瓶颈只在"一轮出不来"，而那是流程问题。
//
// ⚠️⚠️ 实测（2026-08-02，Bedrock Sonnet 4.5，一轮，268 行）：
//   ✅ 单文件 / 零外部依赖 / 语法过 / 限帧 / wallpaperReady / 音频 128 值分段正确
//   ❌ 鼠标**一个事件都没接**（全文只有一个 addEventListener，是 resize）
//   ❌ 没有 dpr 处理
//   ⟹ 一轮已经过了大半硬约束，剩下的全是"清单式缺陷" —— 正好是能收敛的类型。
//
// ⚠️⚠️⚠️ **而"模型审模型"的结论不能直接采信** —— 同一次实测里我让模型审自己的
//   产物，它判错了两次，方向还相反：
//     · 鼠标那条（真的完全没接）它判 **pass** —— 漏报
//     · Math.random 那条（只在粒子诞生时随机，是对的）它判 **fail** —— 误报
//   ⟹ **能用代码判的必须用代码判。** `grep e.buttons` / `node --check` /
//     `grep -c addEventListener` 这几条零成本、零假阳性，而它们当场把漏报逮住了。
//   ⟹ 所以闸门分层，而模型那层**只当建议不当闸门**：
//       ① 机器闸门（这个文件，确定性）
//       ② 真跑闸门（隐藏窗口 3 秒：JS 报错 / fps / 是不是纯黑 —— 在 main.js）
//       ③ 模型审美（"好不好看" —— 只有这层交给模型，且不否决）
//
// 云端能验的：①（纯函数）。云端验不了的：②（要 Electron 窗口）。
// ⟹ ① 全部抽成纯函数放这里，②的判定逻辑也放这里（只收数据不开窗口），
//   这样窗口那边只负责"跑起来收集数据"。

'use strict';

// ⚠️ 配色/亮度曲线（算出来的，不让模型猜 —— 见那个文件的头注释）
const Palette = require('./palette.js');

// ---------------------------------------------------------------------------
// ⚠️⚠️⚠️ **骨架契约**（0.9.140 起，模型面对的是这个，不是宿主接口）
// ---------------------------------------------------------------------------
//
// 用户 2026-08-02 定的架构：**固定骨架 + 模型只填变化**。
// ⟹ 模型现在只写一个 `scene.js`（`window.SCENE` 那两三个函数），
//   而 WebGL/相机/渲染循环/限帧/音频算法/宿主接线**全部由骨架管**。
//
// ⚠️ 判据：把"会坏的部分"写死、验过（实测：一次性写整个 index.html 三轮
//   收敛出的是 300 行 canvas，而参考壁纸是作者 v1→v15 迭代出来的）。
//
// ⚠️⚠️ 而这段**必须和 `wallpaper/skeleton/runtime.js` 里真的给出去的 ctx 对得上** ——
//   错一个字段名，模型写的代码就是 `undefined.xxx`（而那是运行时才炸）。
//   ⟹ 有守卫盯着（见 gen.test.js）。
const SKELETON_API = `# 你要写的是 \`scene.js\`，只挂一个 \`window.SCENE\`

\`\`\`js
window.SCENE = {
  build(ctx)     { /* 建元素，加进 ctx.scene */ },
  frame(ctx)     { /* 每帧更新 */ },
  reconfig(ctx)  { /* 可选：参数变了要重建元素个数时 */ },
  layout(ctx)    { /* 可选：窗口尺寸变了要重排时 */ },
};
\`\`\`

## ctx 里有什么（骨架给的，全部可直接用）

| 字段 | 是什么 |
|---|---|
| \`ctx.THREE\` | three.js r128 的全部命名空间（**别自己 import**，它已经在了） |
| \`ctx.scene\` | THREE.Scene（已建好，把你的 Object3D \`add\` 进去） |
| \`ctx.camera\` | THREE.PerspectiveCamera（fov 52，aspect 自动跟着窗口）**你可以改它的位置/fov/lookAt** |
| \`ctx.renderer\` | THREE.WebGLRenderer（已建好、已 setSize，**别自己 render**，骨架每帧会调） |
| \`ctx.defaultLights\` | 装着默认灯光的 Group ⟹ 想换整套打光：\`ctx.scene.remove(ctx.defaultLights)\` 再加自己的 |
| \`ctx.audio\` | \`{ bass, mid, treble }\` 都是 0..1；\`bins[64]\` 是逐段值（低频→高频）；\`everGot\` 有没有收到过音频 |
| \`ctx.track\` | \`{ title, artist, thumbnail, primaryColor, playing }\`；primaryColor 是 \`[r,g,b]\`（0..1）或 null |
| \`ctx.pointer\` | \`{ x, y, down, dragX, dragY }\`；x/y 是 0..1；dragX/dragY 是累计拖拽像素 |
| \`ctx.opts\` | project.json 里那些参数（颜色已转成 \`[r,g,b]\` 数组）**每个都要有默认值** |
| \`ctx.t\` / \`ctx.dt\` | 秒（dt 已经夹了上限 0.1 并乘过 speed） |
| \`ctx.W\` / \`ctx.H\` | 画布 CSS 尺寸 |
| \`ctx.warn(msg)\` | 报一句进诊断报告（出问题时用户能看到） |

## 骨架已经做完的（**你别重做**）
限帧 40fps / dpr 上限 2 / resize / 音频 128 段的解析和峰值累积 / 宿主的
5 个 register 接线 / 未捕获异常上报 / 3 秒自检 / \`wallpaperReady()\`

## ⚠️ 硬要求
1. **只输出 scene.js 的内容**，一个 IIFE 包起来，挂 \`window.SCENE\`。
2. **不许**出现 \`new THREE.WebGLRenderer\` / \`requestAnimationFrame\` /
   \`renderer.render\` / \`addEventListener('resize'\` —— 那些骨架在做，重做会打架。
3. **不许**任何 import / require / fetch / CDN —— three 已经在 \`ctx.THREE\`。
4. 元素数量多时用 \`InstancedMesh\`（N 个 Mesh = N 次 draw call ⟹ 帧率崩）。
   ⚠️ 改了 instance 的 matrix/color 之后**必须**置
   \`needsUpdate = true\`，否则画面完全静止而不报错。
5. **每帧的位置不许用 Math.random()** —— 那看起来是雪花噪点。
   位置要么由索引+时间决定，要么存进数组各自积分运动（诞生时随机是对的）。
6. **没音乐时也要好看**（\`ctx.audio.everGot\` 可能一直是 false）——
   加一个自走的运动（呼吸/漂移/自转），音频只是叠加在上面。
7. 不许 alert / confirm / prompt / debugger。`;

// ---------------------------------------------------------------------------
// 契约：喂给模型的接口说明（宿主层 —— 骨架在用，模型一般不直接碰）
// ---------------------------------------------------------------------------
//
// ⚠️⚠️ 这段**不是我凭记忆写的 prompt** —— 它逐条对应 `we-preload.js` 里
//   `contextBridge.exposeInMainWorld` 真的暴露出去的东西。
//   ⚠️ 判据：喂给模型的契约错一个字，产出就是"看起来对但接不上"，
//     而那种失败查起来比白屏烦得多（画面在动，只是永远不跟音乐）。
//   ⟹ 改 we-preload.js 的接口时**必须同步改这里**，有守卫盯着（见 gen.test.js）。
const CONTRACT = `# 宿主提供的接口（这是真实契约，逐条对应播放器的实现）

window.wallpaperRegisterAudioListener(cb)
  cb(data) —— data 是 128 个 0..1 的数：左声道 0-63、右声道 64-127，每声道低频→高频。
  ⚠️ 取两声道对应位置的平均再分频段。回调比渲染帧快 ⟹ 用 Math.max 累积峰值，
     直接赋值会让两帧之间的峰值被抹掉（症状是"不跟音乐"）。

window.wallpaperRegisterMediaPropertiesListener(cb)
  cb({ title, artist, albumTitle })

window.wallpaperRegisterMediaThumbnailListener(cb)
  cb({ thumbnail, primaryColor, textColor })
  thumbnail 是 data URL（可能是空字符串 —— 拿不到封面是常态，不是异常）
  primaryColor 是 "r g b" 三个 0..1 浮点拼的字符串，不是 #rrggbb

window.wallpaperRegisterMediaPlaybackListener(cb)
  cb({ state }) —— 0=播放 1=暂停 2=停止
  也可以读 window.wallpaperMediaIntegration.PLAYBACK_PLAYING（是 0）

window.wallpaperRegisterMediaTimelineListener(cb)
  cb({ position, duration }) —— 单位是秒

window.wallpaperReady()
  起来了就调它。宿主靠它判断"页面里的 JS 真的活着" ——
  那是区分"白屏因为加载失败"和"白屏因为渲染有问题"的唯一观测点。

鼠标：宿主把桌面上的真鼠标事件注入到 window（壁纸层收不到原生事件）。
  mousemove / mousedown / mouseup / wheel / click
  ⚠️ 拖拽必须靠 e.buttons（位掩码，1=左键）判断，不能只监听 mousemove ——
     宿主把"拖拽中的移动"注入成带 button 的 mouseMove。

可调参数（可选）：挂 window.wallpaperPropertyListener = { applyUserProperties(props) {} }
  props 的每个键是 { value: ... }。这个方向是页面挂给宿主的，和上面几个相反。`;

// ⚠️⚠️ 硬约束 —— 每一条都有**具体的踩坑来历**，所以每条都带上"为什么"。
//   不带理由的约束模型会当成偏好，带了理由它会当成需求。
const CONSTRAINTS = `# 硬约束（每条都不能破）

1. **单个 index.html，自包含。** 不许有 CDN / import / <script src> / <link href> /
   任何网络请求 / 任何外部文件。壁纸跑在自定义协议下，而用户机器可能没网。
   ⟹ 不许用 React / Three.js / p5 等需要打包或外链的库。

2. **只用 canvas 2d 或 WebGL（shader 内联在 HTML 里）。**
   ⚠️ 优先 canvas 2d：WebGL 的失败模式（着色器编译失败 / context 丢失 / 驱动差异）
      全都表现为黑屏，而壁纸层没有 devtools 入口。

3. **必须限帧，上限 40fps。** 它是常驻后台进程。
   写法：requestAnimationFrame 里 if (now - last < 1000/40) return;
   ⚠️ 这个项目为"壁纸吃满 CPU"栽过一轮（ctx.filter 每秒 1.3G 像素，把主线程挤死）。

4. **canvas 尺寸跟随 resize，devicePixelRatio 上限 2。**
   dpr = Math.min(window.devicePixelRatio || 1, 2)
   ⚠️ 3 倍屏上按 3 渲染 = 2.25 倍像素量，对常驻进程不值得。

5. **每个 window.wallpaperXxx 都要 if 保护。** 这个页面可能被直接在浏览器里打开
   （开发时），那时它们全不存在 ⟹ 要能跑（静止也行），不能白屏报错。

6. **每帧的粒子位置不能用 Math.random() 现算。** 随机的话粒子每帧跳到别处，
   看起来是雪花噪点而不是飘浮的尘。
   ⟹ 要么位置由索引和时间决定，要么把粒子存进数组各自积分运动（诞生时随机是对的）。

7. **没有音乐时也要好看。** 拿不到音频/封面/歌名是常态（用户可能没在放歌）。
   ⟹ 静止状态必须是一个完整的画面，不是一个空洞或者一片纯黑。

8. **不许 alert / confirm / prompt / debugger。** 壁纸层没人能点掉那个框。

9. **不许 setInterval 画帧。** 用 requestAnimationFrame ——
   后台标签页里 setInterval 会堆积，而壁纸窗口可能被系统降频。`;

// ---------------------------------------------------------------------------
// 提示词
// ---------------------------------------------------------------------------
//
// ⚠️⚠️ `buildScenePrompt`（0.9.140 那版"一次性生成整个场景"的提示词）
//   **0.9.148 删了** —— 生产流程从 0.9.142 起就是两步
//   （`buildPlanPrompt` 设计 → `buildImplementPrompt` 写代码），
//   而那个函数只剩测试在调它。
//   ⟹ 判据：**只有测试在用的函数是死代码**，而它会漂（我刚就把一段
//     重要的提示词插进了它里面，插完发现生产路径根本不经过那儿）。

// ═══════════════════════════════════════════════════════════════════════════
//  ⚠️⚠️⚠️ **拆成三步**（0.9.142）。用户 2026-08-02：
//    「一次有偷看上限，说明他一次干不下来这个活，那我们都已经有骨架了，
//      完全可以把这个活做拆分吗？多用几次模型就 OK 了呀，然后有一个 AI 自己的
//      工作区中间文件到文档留存方便，就是一步一步可以衔接上就行了呀」
//
//  ⚠️ 而这修的是一件**实测发生过**的事：一次调用要模型同时决定
//    "长什么样"+"怎么写代码"，而推理模型在前半件事上就把预算烧光了
//    （69,841 字 reasoning_content、正文一个字没写）。
//
//  ⟹ 三步，每步的产物落在**工作区**里（`plan.md` / `scene.js`）：
//     ① 设计（写 plan.md）—— 只输出文字，几百 token，不写代码
//     ② 实现（读 plan.md 写 scene.js）—— 设计已经定了，它只管翻译成代码
//     ③ 修（读闸门报告 + scene.js）—— 已有
//
//  ⚠️ 判据：**"想"和"写"分成两次调用，各自的预算就不会互相挤。**
//    而中间产物落盘让"上一步到底想了什么"可回看 —— 那是失败时唯一的线索。
//
//  ⚠️ 而**步骤是写死的**（不是让模型决定下一步做什么）⟹ 这仍然是 workflow。
// ═══════════════════════════════════════════════════════════════════════════
//  ⚠️⚠️⚠️ **构图规则 —— 从用户给的参考壁纸实测出来的**（0.9.145）
// ═══════════════════════════════════════════════════════════════════════════
//
// 用户 2026-08-03：「出来的效果很抽象，不好看…目标就是产出这样的壁纸：
//   粒子效果_网易云监听 这是有逻辑有审美的」
//
// ⚠️⚠️ 而我之前**从没真的拆过那份参考壁纸** —— 只凭"粒子效果"这个名字写提示词。
//   拆完发现我搞错了方向：关键不在"粒子多不多"，在**构图**。
//
// 实测（`preview.gif` 200 帧，逐帧量化亮度分布和三分带）：
//   近黑(<25) 占 17-45%（随音乐呼吸）、高亮(>120) 只占 4-21%
//   上 1/3 亮度恒定 ~25（几乎不变）⟹ 那是背景，主体不占它
//   中 1/3 是主体（58 → 110，音乐驱动）
//   下 1/3 大幅波动（15 → 71）⟹ 波纹扩散到前景
//   视觉：**贴近地平线的低视角**、中心暖白 → 中层紫 → 外围没入黑底
//
// 而它的 `project.json` 有 **44 个属性**，分成七组：
//   外观 / 音频响应 / 视角 / 空闲波浪 / 流星 / 波纹 / 播放器
//   ⟹ **好看是七组各自独立的机制在同一画面里叠加**，不是"一个漂亮场景"。
//
// ⟹ 判据：**"有审美"要拆成能执行的数字，否则它只是个形容词。**
//   我之前的提示词里"相机高度/低视角/地平线/留白/明暗/分层/构图/负空间/
//   景深/对比"**十个词一个都没有** —— 而那正是第一眼看到的东西。
const COMPOSITION = `# ⚠️⚠️⚠️ 构图（**这一节最重要** —— 它决定"好不好看"）

这些数字是从一张公认好看的音乐可视化壁纸上**实测**出来的，照着做：

## ① 画面要有大片"空"
- **近黑区域占 20-45%**（安静时接近 45%，音乐激烈时降到 20%）
- **高亮区域只占 5-20%** —— 亮的地方是少数，那才叫"亮"
- ⚠️ 元素铺满整个画面 = 一片噪点，那是"抽象不好看"的头号成因

## ② 主体在画面中间那一横条，不是正中央一团
- 上 1/3：**几乎全暗**（就是背景/天空，主体不要伸进去）
- 中 1/3：**主体在这里**，最亮
- 下 1/3：前景，可以有扩散出来的东西（波纹/倒影），但比中间暗

## ③ 相机压低，贴近地平线
- ⚠️ **别俯视看全景**（那是"几何演示"的观感，不是壁纸）
- 相机高度**不超过场景尺度的 1/4**，lookAt 的点在**地平线附近**
- 参考：场景半径 20 ⟹ 相机 \`(0, 3~5, 22)\`，看向 \`(0, 1.5, 0)\`
- 这样地平线落在画面上 1/3 处，上面自然留出暗空

## ④ 明暗至少三层，而且是**由中心向外衰减**
- 中心：最亮、偏白（比如 L 0.75）
- 中层：主色饱和（L 0.45）
- 外围：**没入底色**（L 0.15，靠雾或者按距离降亮度）
- ⚠️ 所有元素一个亮度 = 平的，那是第二号"不好看"的成因

## ⑤ 元素要多而小，不要少而大
- **上千个小东西**（InstancedMesh，2000-8000 个），不是几十个大方块
- 单个元素在画面上只有几个像素 ⟹ 它们**合起来**形成质感
- ⚠️ 16×16=256 根柱子那种是"看得清每一根"⟹ 观感是图表，不是风景

## ⑥⚠️⚠️⚠️ 动态：**五组，缺一条就是没做完**（硬规则）

用户 2026-08-03 看完实测产物的原话：「动态的部分太少了」。
参考壁纸有**七组**（地形起伏 / 同心波纹 / 流星 / 空闲波浪 / 强调色 / 相机浮动 /
主题轮换）—— 那是它"活着"的全部来源。

你**必须**做到下面五条，一条一条对着写：

1. **持续**：每帧地形高度跟频谱走（每个点取自己那一段）
   缺了 ⟹ 画面是静的
2. **事件**：bass 过阈值 ⟹ 从中心放一圈波纹，自己扩散→衰减→消失
   缺了 ⟹ 观感就是"它在那转圈"
3. **粒子**：treble 触发 ⟹ 少量亮点向上迸发，2 秒内消失
   缺了 ⟹ 没有"闪"，画面发闷
4. **慢**：周期 ≥20 秒的相机浮动（高度 ±1）+ 色相缓慢漂移
   缺了 ⟹ 看久了死板
5. **空闲**：没音乐时的行波（\`sin(t*1.1 - dist*0.34)\`），
   ⚠️⚠️ **和有音乐时叠加，不是二选一**
   缺了 ⟹ 静音时像坏了

⚠️⚠️ 第 5 条最容易做错：把空闲动画写在 \`else\` 分支里 ⟹ 有音乐时它就没了，
   而那正好抽掉了"整片都在起伏"的那一层（实测栽过）。
   ⟹ **空闲波要一直在**，音频是**叠加**上去的。

⚠️ 而这五组的**驱动量要各自独立** —— 别用同一个 \`t * 系数\` 驱动全部，
   那样它们会同步、看起来像一个动作。

## ⑧⚠️⚠️ 别落回这些"默认"（每一条都是实测栽出来的）

看到自己在写这些就停下来：
- ⛔ **中心一个尖锥** —— 高度 = f(到中心距离) ⟹ 那是几何体不是风景（见 ③）
- ⛔ **雾密度自己填数字** —— \`FogExp2(bg, 0.12)\` + 相机 12m ⟹ 透光率 **9%**，
  整片被乘掉 91%，而那时**调 L 完全无效**（雾在 setHSL 之后作用）
- ⛔ **亮度连乘** —— \`lum *= ...\` 两次 ⟹ 外圈纯黑而不是"暗"
- ⛔ **高饱和霓虹紫** —— S > 0.7 一眼就俗（目标中位 0.30-0.34）
- ⛔ **整体缩放/旋转当主动态** —— 观感是"呼吸的球"，跟音乐没关系
- ⛔ **元素少而大**（几百个）—— 看得清每一个 ⟹ 图表感
- ⛔ **俯视看全景** —— 相机高度 > 距离的 1/4 ⟹ 几何演示的观感
- ⛔ **空闲动画写在 else 里** —— 有音乐时它就消失了

## ⑦ 音频要**局部驱动**，不是整体驱动
- ⚠️ "所有元素一起变大/一起转" = 看起来像呼吸的球，跟音乐没关系
- 对的做法：**每个元素取自己那一段频谱**（按它到中心的距离、或者按索引取 bin）
  ⟹ 那样画面上能看出"音域的形状"，而那才是"跟着音乐"的观感`;

// ⚠️⚠️⚠️ **这份提示词就是产品本身**（0.9.146）。用户 2026-08-03：
//   「我们这个就是你不断自己调整内置提示词吗？以后我说生成，你已经给我个
//     预置提示词…我一看生成我想要的这个效果了，然后就说明现在这个词至少在
//     这张壁纸上是 OK 了，然后我们再泛化，那这种类型的壁纸不就被我们拿下了吗」
//
// ⟹ 判据：**"我怎么把它调好"是开发过程，不是运行时状态机。**
//   0.9.140-145 我建了五维配方枚举 + 读历史避重 + 前三张固定配方 ——
//   那全是把开发过程暴露成产品行为。拆了。
//
// ⟹ 现在只有一份内置提示词。它的目标很具体：**一次出参考壁纸那个效果**
//   （`粒子效果_网易云监听`，用户给的）。下面每个数字都是从它的
//   `preview.gif` 200 帧逐帧量化出来的，不是我编的审美主张。
function buildPlanPrompt(userWant) {
  return `你要为一个 macOS 动态壁纸设计一个 3D 场景。**这一步只做设计，不写代码。**

# 要做的东西

一片**随音乐起伏的发光地形**。低视角看过去像夜色里的丘陵，低频砸下来时
从中心放出一圈同心波纹扫过整片地形。安静时地形缓慢呼吸，像有风吹过。

${COMPOSITION}

# ⚠️⚠️ 目标的实测数据（这些数字来自一张公认好看的壁纸，照着做）

- **底色 #070815**（近黑，偏蓝紫）
- **主色相**：安静时洋红紫 H 290-330，音乐激烈时偏向蓝紫 H 210-260
- ⚠️⚠️ **饱和度 S 中位 0.30-0.34**，只有最激烈的瞬间才升到 0.65
  ⟹ **低饱和是它"高级"的关键**。高饱和霓虹色一眼就俗，别用。
- 亮度 V 中位 0.35（安静）→ 0.65（激烈）
- **8000 个左右**的小方块/小点（InstancedMesh 一次画完），铺在 XZ 平面
- 场景半径约 20，相机在 **(0, 4, 22)** 看向 **(0, 1.5, 0)**（贴近地平线）

## ⚠️⚠️⚠️ 它是"一整片起伏的地形"，**不是"中心一个尖锥"**

这是最容易做错的一条（实测三次都错在这）：
- ⛔ **错的**：\`高度 = f(到中心的距离)\` ⟹ 那必然是个圆锥/圆丘，
  中间一个尖、四周越来越低。观感是"一个几何体"，不是风景。
- ✅ **对的**：\`高度 = f(该点自己的频段能量) + f(位置的噪声/波函数)\`
  ⟹ 整片都在起伏，高低**随位置随机分布**，像丘陵。
  ⚠️ 到中心的距离只用来**选哪个频段**（低频在中间、高频在外圈），
    **不直接决定高度** —— 那两件事要分开。

⟹ 具体做法：
  · 每个点按 \`距离 → bin 序号\` 取自己那一段频谱（这是"音域的形状"）
  · 再叠一层跟位置有关的噪声或正弦（\`sin(x*0.3) * cos(z*0.4)\` 这类）
    ⟹ 那让同一圈上的点高度也不一样，画面才有"地形"的质感
  · 元素**均匀铺满**（网格排布，或者等面积分布），**不要中心密外圈疏**
    ⚠️ \`r = R * sqrt(i/N)\` 那种是"中心密"，会强化"一个圆丘"的观感

## ⚠️⚠️ 亮度衰减**只做一次**（这是"一片黑"的另一个成因）

- 中心最亮偏白（L 0.75-0.9）→ 往外降到 L 0.25 左右 → 最外圈靠**雾**没入底色
- ⛔ **不许连乘**：\`lum *= (1-d)\` 然后又 \`lum *= (0.1 + 0.9*(1-d))\`
  ⟹ 那是二次/三次衰减，外圈会变成**纯黑**而不是"暗"
  ⚠️ 实测出过这个：外圈乘到 0.1，画面上高亮占比 **0%**
- ✅ 一次线性或者一次平方就够：\`L = 0.85 - 0.6 * d\`（d 是 0..1 的归一化距离）
- ⚠️ 而**中间那一带要足够宽**（不是只有正中心几个点亮）——
  目标是高亮像素占 5-20%，那需要一片而不是一个点

# 用户这次的补充要求
${userWant || '（没特别要求，就按上面那个做）'}

⚠️ 用户的要求**优先于**上面的描述（比如他说"要绿色的"就用绿色），
   但**构图那七条不许动** —— 那是"好不好看"的地基，不是风格偏好。

# 输出：一份设计说明（markdown，**不要代码**）

按这几个小标题写，每条都要**具体到能直接照着写代码**：

## 构图
相机位置和 lookAt（**压低**）、近黑占多少、主体在画面哪一横条、明暗三层的亮度

## 元素
多少个（**几千**）、什么几何体、多大、怎么排（给出坐标公式）

## 环境
底色（hex）、雾（类型 + near/far 或密度）
⚠️⚠️ **灯光和材质要配套说清**（这是"画面一片黑"的头号原因）：
   · 用 MeshBasicMaterial（自发光）⟹ 不需要灯，说"不打灯"
   · 用 MeshStandardMaterial / Phong ⟹ **必须有灯**（没灯是纯黑，不是暗）
     ⟹ 那就说清几个灯、什么类型、颜色、方向、强度

## 配色
主色和辅色的 HSL 范围、**亮度和饱和度怎么随"到中心的距离"变**

## 音频映射
bass / mid / treble 各驱动什么，具体数值范围。
⚠️ 必须说清**每个元素怎么取到自己那一段频谱**

## 三组独立动态
一个持续的、一个事件触发的（自己扩散消失）、一个慢的（周期 20 秒以上）

## 空闲动画
没有音乐时怎么动（**必须有** —— 否则用户会以为坏了）

⚠️ 400-700 字。这是给下一步写代码用的规格书，不是文章。
⚠️⚠️ **直接开始写那几个小标题，不要先长篇分析**（也不要复述上面的要求）——
   实测过：推理模型在这一步烧了 6,465 字思考、正文一个字没写就撞上预算了。
⚠️⚠️ 而**构图那几条是硬要求** —— 元素排得再巧，俯视看全景 + 铺满画面
   还是会得到"很抽象、不好看"的结果。`;
}

// ⚠️ 第二步：拿着自己的设计写代码。
//   ⚠️⚠️ 这一步**不再给它配方和历史** —— 那些已经消化进 plan 了，
//     再给一遍会让它重新做设计决定（而那正是我们要避免的"一次干两件事"）。
//   ⚠️ 判据：**每一步只给它这一步需要的东西。** 多给的信息会被当成任务。
// ═══════════════════════════════════════════════════════════════════════════
//  ⚠️⚠️⚠️ **把骨架的真源码给模型看**（0.9.145）
// ═══════════════════════════════════════════════════════════════════════════
//
// 用户 2026-08-03：「我们这个壁纸渲染器本来就是我们做，所以代码相关的一些
//   ⟨说明⟩需求什么的也可以告诉我们的模型，使得它更好地完成我们的目标，
//   这是我们很大的优势」
//
// ⚠️⚠️ 而这个优势我一直在浪费：给模型的是 `SKELETON_API` —— **我手写的一份摘要**。
//   查了一下，那份摘要已经漂了：漏了 `MIN_DT`（限帧值）/ `__dpScene`（观测点）/
//   `frameErrors`（连续报错就停渲染）三样。
//   ⚠️ 判据：**手写的摘要必然漂移，而真源码不会。**
//     摘要漂移的代价不是"少说了点什么"，是**模型按错的假设写代码**
//     （比如不知道限帧 40 就自己去插值、不知道 frameErrors 会停就不管异常）。
//
// ⟹ 给它真源码。⚠️ 而**只给 runtime.js**（13KB）——
//   `index.html` 和 `three.js` 它不需要（一个是壳、一个是库）。
//   ⚠️ 13KB 约 4k token，而写代码那步预算 24000 ⟹ 装得下，
//     而它换来的是"模型看到的和实际跑的是同一份东西"。
function readSkeletonSource() {
  const fs = require('fs');
  const path = require('path');
  try {
    return fs.readFileSync(
      path.join(__dirname, '..', 'skeleton', 'runtime.js'), 'utf8');
  } catch {
    // ⚠️ 读不到就退回摘要 —— 那比整个流程失败好。
    //   ⚠️ 但要让调用方知道（它会在日志里说），否则"模型看的是二手信息"
    //     这件事会静默发生。
    return null;
  }
}

function buildImplementPrompt(plan, example, skeletonSource) {
  // ⚠️⚠️ **优先给真源码** —— 这个播放器是我们自己写的，那是这个项目
  //   相对"让模型盲写壁纸"的最大优势（用户 2026-08-03 点出来的）。
  //   ⚠️ 而摘要只当兜底（读不到文件时）。
  const skeleton = skeletonSource
    ? `# ⚠️⚠️ 骨架的**真实源码**（\`runtime.js\` —— 你的 scene.js 会被它调用）

这是**实际会跑的那份代码**，不是摘要。看清它给你什么、什么时候调你：

\`\`\`js
${skeletonSource}
\`\`\`

⚠️ 从上面这份源码里你能直接确认：
   · \`ctx\` 里到底有哪些字段（别猜）
   · 它每帧调 \`SCENE.frame(ctx)\` 之后自己会 \`renderer.render\` ⟹ 你别再调
   · 限帧 40fps（\`MIN_DT\`）⟹ 你不用自己做时间控制
   · \`SCENE.frame\` 连续抛异常 30 次它会**停止渲染** ⟹ 别让 frame 抛
   · 音频是累积峰值的（\`Math.max\`）⟹ 你读到的 bass 已经是峰值，不用自己平滑

# ⚠️⚠️⚠️ 灯光和材质必须配套（这是"画面一片黑"的头号原因）

骨架默认往场景里放了 \`ctx.defaultLights\`（一个环境光 + 一个平行光）。
你**可以**删掉它换自己的打光 —— 但**删了之后材质必须跟着换**：

| 材质 | 需要灯吗 | 没灯的结果 |
|---|---|---|
| \`MeshBasicMaterial\` | **不需要** | 正常显示（自发光、扁平霓虹感） |
| \`MeshStandardMaterial\` | **需要** | **纯黑**（字面的 0,0,0，不是"暗"） |
| \`MeshPhongMaterial\` / \`Lambert\` / \`Physical\` / \`Toon\` | **需要** | **纯黑** |

⟹ 三条合法组合，选一条：
① \`scene.remove(ctx.defaultLights)\` + \`MeshBasicMaterial\`（自发光，霓虹感）
② \`scene.remove(ctx.defaultLights)\` + **自己加灯** + Standard/Phong
③ **留着 defaultLights** + Standard/Phong（最省事）

⚠️ 而"删了灯 + Standard 材质 + 不加灯"= 必然全黑，检查器会拦住它。

# ⚠️ 另外两个"什么都不显示"的常见原因
· 建了 Mesh **没有 \`ctx.scene.add(...)\`** ⟹ 它不在场景里（不报错）
· 元素坐标在相机背后或视锥外 ⟹ 相机在 (0,4,22) 看向 (0,1.5,0)，
  所以元素应该在**原点附近**（X/Z 大致 -20..20，Y 大致 0..10）`
    : SKELETON_API;

  return `照下面这份设计说明写 scene.js。**设计已经定了，你只管把它翻译成代码。**

# 设计说明（你上一步写的）
${plan}

${skeleton}

# ⚠️⚠️⚠️ 配色和亮度：**用下面这段现成的，别自己推公式**

把这段**原样抄进你的 scene.js 顶部**（IIFE 里面），然后调它：

\`\`\`js
${Palette.INJECT}\`\`\`

⟹ 用法：
· 底色：\`ctx.scene.background = new THREE.Color(DP.bg)\`
· 每个元素的颜色：
  \`const d = dist / RADIUS;\`  （0..1，到中心的归一化距离）
  \`color.setHSL(DP.hue(d, loud) / 360, DP.sat(d, loud), DP.lum(d, energy));\`
  （\`loud\` 用 \`audio.bass\` 或三段平均，\`energy\` 用该点那一段频谱值）

· 雾：\`ctx.scene.fog = new THREE.FogExp2(DP.bg, DP.fogDensity(相机距离, 场景半径))\`

# ⚠️⚠️⚠️ 这段是**强制**的（检查器会拦）

**不许自己推配色/亮度/雾密度的公式。** 两次实测都栽在这上面：
① 自己推亮度 ⟹ 写成三次衰减（\`lum\` 被 \`*=\` 两遍）⟹ 外圈纯黑、高亮占比 **0.0%**
② 自己填雾密度 0.12 + 相机在 12m ⟹ 透光率 **9%** ⟹ 整片被乘掉 91%
   ⚠️ 而那次连着三轮都在调 L —— **因为雾在 setHSL 之后作用，调 L 完全无效**

⟹ **DP.lum 已经把"中心偏白、外围没入底色"算好了 ⟹ 别再乘任何系数。**
⟹ **DP.fogDensity 算出的密度让最外圈透光 25%、近处 90%** —— 那才是"没入底色"，
   而不是"整片变暗"。

⚠️ 你**可以**在 DP 之外叠自己的东西（比如给亮核额外 +0.1、给波纹加亮），
   但**基线必须是 DP** —— 加法可以，乘法不行。

# 参考实现（**看它的写法，不是抄它的设计** —— 它的设计和你的不一样）
\`\`\`js
${example}
\`\`\`

# 输出
只输出 scene.js 的完整内容（一个 IIFE，挂 window.SCENE）。
⚠️ **包括上面那段 DP**（原样抄进去，别改里面的数字）。
⚠️ 直接开始写代码，不要复述设计、不要解释 —— 设计已经在上面了。
不要 markdown 围栏。`;
}

// ═══════════════════════════════════════════════════════════════════════════
//  ⚠️⚠️⚠️ **观测报告 —— 给模型看的"眼睛"**（0.9.149）
// ═══════════════════════════════════════════════════════════════════════════
//
// 用户 2026-08-03：「就是 agent 的 skills / tools 这些东西肯定都是要正常的
//   配给他的，包括说什么 runtime 这些东西」「思考方式 react 可能比较好」
//
// ⚠️⚠️ 而现在的架构是：模型盲写 → 我们判 → **回喂一句结论**。
//   那不是 ReAct，那是"改作业"。ReAct 的关键是模型能**自己观察**，
//   而观察到的东西要是**原始数据**，不是我的判断。
//
// ⟹ 判据：**把观测数据原样给它，让它自己说诊断。**
//   我的判断（`C-太暗了`）会把它往一个方向推；而原始数据
//   （"上/中/下三带亮度 8/28/9，高亮 0%，你的代码里 lum 被 *= 了两次"）
//   让它能自己发现"哦，我把亮度乘了两遍"。
//
// ⚠️ 而这**不是把闸门换掉** —— 闸门照旧（那是确定性的、拦"必然出事"的）。
//   这一层是**在闸门之外多给它一份现场**。
function buildObservation(probe, code) {
  const p = probe || {};
  const px = p.sampledPixels;
  const lines = [];

  lines.push('# 你上一版真跑了 3 秒，下面是**实际观测到的数据**（不是我的判断）');
  lines.push('');

  // ── ① 运行时
  const gl = p.webgl || {};
  const fps = p.ms > 0 ? (p.frames / (p.ms / 1000)) : 0;
  lines.push('## 运行时');
  lines.push(`· 画了 ${p.frames} 帧 / ${p.ms}ms（约 ${fps.toFixed(1)} fps）`);
  lines.push(`· WebGL context：${gl.context ? '正常' : '**没建起来**'}`
    + `，gl.getError() = ${gl.glError === undefined ? '?' : gl.glError}`);
  // ⚠️⚠️ 场景对象数是最有信息量的一个 —— 骨架默认放了一个 defaultLights Group
  //   ⟹ 只有 1 个就说明模型删了灯或者什么都没加（实测那次就是）
  lines.push(`· 场景里有 ${gl.objects} 个顶层对象`
    + (gl.objects === 1
      ? '（⚠️ 骨架默认放了一个 defaultLights Group ⟹ **只有 1 个说明你要么删了它、'
        + '要么什么都没 add 进场景**）'
      : ''));
  if ((p.errors || []).length) {
    lines.push('· ⚠️ 页面里报的错（原文）：');
    for (const e of p.errors.slice(0, 6)) lines.push(`    ${String(e).slice(0, 300)}`);
  }
  lines.push('');

  // ── ②⚠️⚠️ 像素观测 + **目标区间**（那让它能自己判差多少）
  if (px && px.total) {
    const pct = (n) => `${((100 * n) / px.total).toFixed(1)}%`;
    lines.push('## 画面（32×32 采样，共 1024 点）');
    lines.push(`· 近黑(<25)  ${pct(px.black)}    ← 目标 20-45%`);
    lines.push(`· 高亮(>120) ${pct(px.bright || 0)}    ← 目标 5-20%`);
    if (Array.isArray(px.bands)) {
      lines.push(`· 三分带平均亮度  上 ${px.bands[0].toFixed(0)}`
        + ` / 中 ${px.bands[1].toFixed(0)} / 下 ${px.bands[2].toFixed(0)}`
        + '    ← 目标 25 / 90 / 50（中间最亮，上面几乎全暗）');
    }
    if (Number.isFinite(px.satMedian)) {
      lines.push(`· 主体饱和度中位  ${px.satMedian.toFixed(2)}`
        + '    ← 目标 0.30-0.34（低饱和才"高级"，高饱和霓虹色一眼就俗）');
    }
    lines.push('');
  } else {
    lines.push('## 画面');
    lines.push('· （没截到图 —— 那是我们这边的问题，不是你的）');
    lines.push('');
  }

  // ── ③⚠️⚠️⚠️ **从你自己的代码里量出来的事实**
  //   ⚠️ 判据：**给"你写了什么"的事实，比给"你错了"的判断有用。**
  //     实测那次：模型自己的注释里写着"根因分析：上一版把 lum 一路乘小"
  //     ⟹ 它诊断对了、但改的时候又犯了一次 ⟹ 说明它需要的是**当场的证据**。
  const facts = measureCode(code);
  if (facts.length) {
    lines.push('## 从你上一版代码里量出来的（事实，不是判断）');
    for (const f of facts) lines.push(`· ${f}`);
    lines.push('');
  }

  return lines.join('\n');
}

// ⚠️ 从代码里量出**可数的事实** —— 不做判断，只报数。
//   ⚠️ 判据：**"你的 lum 被 *= 了 2 次"是事实，"太暗了"是判断。**
//     前者让模型能自己连上因果，后者只能让它去调那个它以为相关的系数。
function measureCode(code) {
  const src = String(code || '');
  if (!src) return [];
  const out = [];

  // 元素个数
  const inst = src.match(/new\s+THREE\.InstancedMesh\s*\([^,]+,[^,]+,\s*([^)]+)\)/);
  if (inst) out.push(`InstancedMesh 的实例数参数是 \`${inst[1].trim()}\``);

  // 相机
  const cam = src.match(/camera\.position\.set\s*\(([^)]*)\)/);
  const look = src.match(/lookAt\s*\(([^)]*)\)/);
  if (cam) {
    out.push(`相机位置 (${cam[1].trim()})`
      + (look ? `，看向 (${look[1].trim()})` : '')
      + '  ← 目标是压低到贴近地平线：高度不超过距离的 1/4');
  }

  // ⚠️⚠️ 亮度被乘了几次 —— 这是实测那次的直接死因
  const lumLines = src.split('\n').filter((l) => /\b(lum|bright|lightness)\b/i.test(l));
  const mul = lumLines.filter((l) => /\b(lum|bright|lightness)\w*\s*\*=/i.test(l));
  if (mul.length) {
    out.push(`亮度变量被 \`*=\` 了 **${mul.length} 次**`
      + `（那几行：${mul.map((l) => l.trim().slice(0, 60)).join(' ｜ ')}）`
      + '  ← 衰减只该做一次；两次以上外圈会变成纯黑而不是"暗"');
  }
  // 有没有用我们给的 DP 曲线
  if (/\bDP\.lum\s*\(/.test(src)) {
    out.push('✓ 用了播放器提供的 DP.lum 曲线');
  } else if (/setHSL|setRGB/.test(src)) {
    out.push('⚠️ 没用播放器提供的 DP.lum/DP.sat/DP.hue，自己推的公式'
      + '  ← 那三个是从目标壁纸实测算出来的，自己推容易出上面那种连乘问题');
  }

  // 材质 + 灯
  const mats = [...src.matchAll(/new\s+THREE\.(Mesh\w+Material)/g)].map((m) => m[1]);
  if (mats.length) {
    const uniq = [...new Set(mats)];
    const removed = /scene\.remove\s*\(\s*(ctx\.)?defaultLights/.test(src);
    const added = /new\s+THREE\.\w*Light\s*\(/.test(src);
    out.push(`材质：${uniq.join(' / ')}`
      + `；${removed ? '**删掉了** defaultLights' : '保留了 defaultLights'}`
      + `；${added ? '自己加了灯' : '**没有**自己加灯'}`);
  }

  // ⚠️⚠️⚠️ **雾吃掉多少** —— 这是模型自己看不见的那一层
  //   （雾在 setHSL 之后作用 ⟹ 它三轮都在调 L 而调不动）
  const fogE = src.match(/new\s+THREE\.FogExp2\s*\([^,]+,\s*([\d.]+)\s*\)/);
  const camZm = src.match(/camera\.position\.set\s*\([^,]+,[^,]+,\s*([-\d.]+)\s*\)/);
  if (fogE) {
    const den = Number(fogE[1]);
    const dist = camZm ? Math.abs(Number(camZm[1])) : 20;
    const t = Math.exp(-Math.pow(den * dist, 2));
    out.push(`雾：FogExp2 密度 ${den}，相机在 ${dist}m`
      + ` ⟹ 那个距离的**透光率 ${(t * 100).toFixed(0)}%**`
      + `（元素亮度被乘掉 ${(100 - t * 100).toFixed(0)}%）`
      + '  ← ⚠️ 雾在 setHSL **之后**作用：你设的 L=0.9 实际显示成 '
      + `${(0.9 * t).toFixed(2)}。合理的透光率在相机处该是 60-90%`);
  }
  const fogL = src.match(/new\s+THREE\.Fog\s*\([^,]+,\s*([\d.]+)\s*,\s*([\d.]+)\s*\)/);
  if (fogL) {
    out.push(`雾：线性 Fog near=${fogL[1]} far=${fogL[2]}`
      + (camZm ? `，相机在 ${Math.abs(Number(camZm[1]))}m` : '')
      + '  ← far 要在场景边缘之外，否则主体整个泡在雾里');
  }

  // ⚠️ 高度里的距离项 —— "像个圆锥"的直接证据
  const heightLine = src.split('\n').find((l) =>
    /\b(target|h|height)\s*=\s*[^=]/.test(l) && /\b(core|dist|dc|rNorm)\b/.test(l));
  if (heightLine) {
    out.push(`高度的算式里用到了"到中心的距离"：\`${heightLine.trim().slice(0, 90)}\``
      + '  ← 距离该只用来**选频段**；高度要靠频段能量 + 跟位置有关的噪声，'
      + '否则画出来是个圆锥');
  }

  return out;
}

// ⚠️⚠️ 回喂：把**机器闸门的原文**给模型，不是我转述的结论。
//   ⚠️ 判据：转述会丢信息（"缺鼠标处理"和"全文只有一个 addEventListener 且是
//     resize"对模型是两种信息量），而丢的那部分正是它需要用来定位的。
function buildRepairPrompt(previousCode, problems, roundInfo, observation) {
  // ⚠️⚠️⚠️ **同一个问题连着两轮没解决 ⟹ 说明改的方向不对**（0.9.147）。
  //   用户 2026-08-03 那次：三轮都在修，而"一片黑"一直没好
  //   （读数 12/9/6 → 三轮后还是 12/9/6）。
  //   ⚠️ 根因是**回喂给的是现象**（"太暗了，把亮度提上去"）⟹ 模型改的是
  //     颜色的 L 值，而真正的原因是"删了灯还用需要光的材质"（纯黑，不是暗）。
  //   ⟹ 判据：**同一个问题第二次出现时，要明说"上一轮那样改没用"** ——
  //     否则模型会继续沿着同一个错方向微调。
  const repeated = (roundInfo && roundInfo.repeated) || [];
  const stuckNote = repeated.length
    ? `

# ⚠️⚠️⚠️ 注意：下面这些问题**上一轮你已经改过一次了，但没解决**
${repeated.map((id) => `· ${id}`).join('\n')}

⟹ 那说明**你上一轮改的方向不对**，别再微调同一个地方。
   停下来想：这个现象的**根因**可能是什么？
   比如"画面太暗"的根因可能是：
     · 删掉了 ctx.defaultLights 却用了需要光照的材质（那是**纯黑**，不是暗）
     · 元素建了但没 ctx.scene.add(...)
     · 元素在相机背后 / 在视锥外（检查坐标范围和相机朝向）
     · 材质的 opacity/transparent 设错
   ⚠️ 换一个假设，别改同一个系数。`
    : '';

  // ⚠️⚠️⚠️ **观测数据排在检查器报告前面**（0.9.149）。用户 2026-08-03：
  //   「思考方式 react 可能比较好」
  //   ⟹ ReAct = 观察 → 思考 → 行动。而我原来是**先给结论**
  //     （"[C-太暗了] 把亮度提上去"）⟹ 模型直接跳到"行动"，
  //     沿着我指的方向微调 ⟹ 三轮都没修掉。
  //   ⟹ 判据：**先给现场，再给结论；而且要求它把诊断说出来。**
  //     说出来那一步不是形式 —— 它强迫模型把"数据"和"我改了什么"连上，
  //     而那正是它上次跳过的一步（它注释里诊断对了，改的时候又犯了一次）。
  const obs = observation ? `${observation}\n` : '';

  return `你上一版写的 scene.js 没通过自动检查。

${obs}# 检查器报告（确定性检查，这些是必须修掉的）
${problems.map((x, i) => `${i + 1}. [${x.id}] ${x.detail}`).join('\n')}${stuckNote}

${SKELETON_API}

# 上一版代码
\`\`\`js
${previousCode}
\`\`\`

# 要做的事
修掉上面每一条。

⚠️⚠️ 报告里 **C- 开头的那些是构图/设计问题**（不是 bug）——
   它们带着具体数字（"近黑只占 5%，参考壁纸是 20-45%"）。
   ⟹ 那种要改的是**设计本身**（元素个数、相机高度、亮度分层、加一种动态），
     不是调一个系数。⚠️ 改一个系数改不动构图。

⚠️ 其余（A- B- D- E- F- R- 开头，以及 C-重做骨架）是 bug ——
   **保留原来的视觉设计**，只修那个具体错误。

# ⚠️⚠️⚠️ 输出格式（两段，顺序不能换）

## 第一段：诊断（3-6 行，普通文字）
先把**观测数据**和**你的代码**连起来，一条一条说：
  · 哪个数字不对（照抄上面那个数）
  · 它是你代码里哪一行造成的
  · 你打算怎么改（改哪一行、改成什么）

⚠️ 这一步**不是形式** —— 实测过：你上一版的注释里写着"根因分析：上一版把
   lum 一路乘小"，说明诊断是对的，**但改的时候又犯了一次同样的错**。
   ⟹ 把因果显式写出来，能让你在动手前发现"我要改的这一行正是我上次改错的那行"。

## 第二段：完整的 scene.js
在诊断之后，用一行 \`===SCENE===\` 分隔，然后是完整代码。
⚠️ 不要 markdown 围栏。
只输出修好的 scene.js 完整内容。
⚠️ 直接开始写代码，不要先长篇分析 —— 预算要留给代码本身。
不要 markdown 围栏、不要解释。`;
}

// ---------------------------------------------------------------------------
// 模型输出的清洗
// ---------------------------------------------------------------------------
//
// ⚠️⚠️ 模型**经常**不听"不要 markdown 围栏"（我实测那轮听了，但那不能当保证）。
//   而一个带 ``` 的 HTML 首行会让整个文件白屏 ⟹ 这里必须兜。
//   ⚠️ 而且要兜"围栏 + 前后废话"两种：有时它会写一句"这是生成的壁纸："再给代码。
//   ⟹ 判据：**从 `<!DOCTYPE` 或 `<html` 切到最后一个 `</html>`** ——
//     那是唯一不依赖模型听话的定位方式。
function extractScene(text) {
  const raw = String(text || '');
  let body = raw;

  // ⚠️⚠️⚠️ **先切掉诊断那一段**（0.9.149）。回喂时要求模型
  //   "先写诊断，再用 ===SCENE=== 分隔，然后是代码"（ReAct 的"思考"那一步）
  //   ⟹ 这里要把分隔符前面的文字扔掉。
  //   ⚠️ 而**没有分隔符时不能报错** —— 第一轮（buildImplementPrompt）
  //     不要求诊断，那时整段都是代码。⟹ 有就切，没有就当全是代码。
  const sep = body.lastIndexOf('===SCENE===');
  if (sep >= 0) body = body.slice(sep + '===SCENE==='.length);

  // ⚠️ 模型经常不听"不要 markdown 围栏"（实测过）⟹ 这里必须兜。
  const fence = body.match(/```(?:js|javascript)?\s*\n([\s\S]*?)```/i);
  if (fence) body = fence[1];
  body = body.trim();

  // ⚠️⚠️ 判据：**必须能看到 `window.SCENE`** —— 那是这个文件唯一的作用。
  //   看不到就说明模型输出的不是我们要的东西（比如它写了整个 HTML，
  //   或者它在解释而不是写代码）。
  if (!/window\.SCENE\s*=/.test(body)) {
    throw new Error('模型没有输出 scene.js（找不到 `window.SCENE =`）。'
      + `它给的开头是：${raw.slice(0, 200)}`);
  }
  // ⚠️ 而如果它写了整个 HTML，那是**理解错了任务** —— 明确报出来，
  //   而不是让一份 HTML 被当成 JS 写进 scene.js（那会白屏）。
  if (/<!DOCTYPE|<html[\s>]|<script[\s>]/i.test(body)) {
    throw new Error('模型输出了 HTML —— 这一版只要 scene.js（骨架那些文件我们自己给）');
  }
  return body;
}

// ⚠️ 把诊断那一段拿出来 —— 它要进日志和 plan 留档。
//   ⚠️ 判据：**"模型怎么想的"是排查时唯一的线索**，而它现在会写出来了
//     ⟹ 别扔掉（我们为"报错不带现场"栽过）。
function extractDiagnosis(text) {
  const raw = String(text || '');
  const sep = raw.lastIndexOf('===SCENE===');
  if (sep < 0) return '';
  return raw.slice(0, sep).trim();
}

// ---------------------------------------------------------------------------
// ① 机器闸门 —— 确定性检查，零假阳性
// ---------------------------------------------------------------------------
//
// ⚠️⚠️ 每一条的判据都是"**破了必然出事**"，不是"最好这样"。
//   偏好类的东西（配色、构图、节奏）一条都不放这里 —— 那些交给用户的眼睛。
//   ⚠️ 判据来源：实测那轮模型漏掉的正是能机器判的（鼠标、dpr），
//     而它自己审的时候把这些判成 pass。
//
// ⚠️ `checkJsSyntax` 由调用方注入 —— 语法检查要 `new Function` 或 vm，
//   而这个模块要能在测试里被纯粹地跑（不带副作用）。
function inspect(code, hooks) {
  const h = hooks || {};
  const problems = [];
  const add = (id, detail) => problems.push({ id, detail });
  const src = String(code || '');

  // ── A. 语法（白屏最常见的原因，而它 100% 能机器判）
  if (typeof h.checkJsSyntax === 'function') {
    const err = h.checkJsSyntax(src);
    if (err) add('A-语法错误', `scene.js 语法错误：${err}`);
  }

  // ── B. 契约：必须挂 window.SCENE 且有 build/frame
  if (!/window\.SCENE\s*=/.test(src)) {
    add('B-没挂 SCENE', '没有 `window.SCENE = {...}` ⟹ 骨架启动时会报"scene.js 没提供 SCENE"');
  }
  for (const fn of ['build', 'frame']) {
    // ⚠️ 两种写法都认：`build(ctx) {` 和 `build: (ctx) =>`
    if (!new RegExp(`${fn}\\s*[(:]`).test(src)) {
      add('B-缺函数', `SCENE 里没有 ${fn}() ⟹ 骨架会拒绝启动`);
    }
  }

  // ── C.⚠️⚠️ **只拦"会打架"和"会坏"的，不拦"我没想到的写法"**
  //
  // 用户 2026-08-02：「我给你的骨架仅仅是说一种想要在有限空间内高质量的
  //   一种手段，本质上我们其实还是 agent，不要太死板了」
  // ⟹ 判据：**闸门守的是"必然出事"，不是"不符合我的预期"。**
  //   一条拦住了正确写法的闸门比没有闸门更贵 —— 它会让模型去改一段本来对的
  //   代码（而这个项目实测栽过：F 闸门连着三轮误报粒子诞生的 Math.random，
  //   模型每次都是对的）。
  const FORBIDDEN = [
    // ── 会和骨架打架的（两个渲染循环 / 两次 setSize ⟹ 撕裂、帧率减半）
    [/new\s+THREE\.WebGLRenderer/, '自己建了 WebGLRenderer —— 骨架已经建好在 ctx.renderer'],
    [/requestAnimationFrame/, '自己起了渲染循环 —— 骨架每帧会调 SCENE.frame，两个循环会打架'],
    [/renderer\.render\s*\(/, '自己调了 renderer.render —— 骨架每帧会调，重复渲染等于帧率减半'],
    [/addEventListener\s*\(\s*['"]resize/, '自己监听了 resize —— 骨架会调 SCENE.layout'],
    // ── 在这个环境里**跑不起来**的
    [/\bimport\s+[\w{*]/, 'ES import —— scene.js 是普通 script，three 已经在 ctx.THREE'],
    [/\brequire\s*\(/, 'require —— 壁纸跑在浏览器环境，而且 three 已经在 ctx.THREE'],
    [/\b(fetch|XMLHttpRequest)\s*\(/, '网络请求 —— 壁纸不许联网（用户机器可能没网，而失败是静默的）'],
    [/<script|<!DOCTYPE|<html[\s>]/i, 'HTML 标签 —— 这个文件是纯 JS（骨架那些文件我们自己给）'],
    // ── 用户体验上不能有的
    [/\b(alert|confirm|prompt)\s*\(/, '模态框 —— 壁纸层在桌面最底下，没人能点掉'],
    [/\bdebugger\b/, 'debugger 语句'],
  ];
  for (const [re, msg] of FORBIDDEN) {
    if (re.test(src)) add('C-重做骨架/禁用', msg);
  }

  // ⚠️⚠️⚠️ **而下面这些是"允许的"，写在这里防止以后有人把它们加进闸门**：
  //   · 换整套灯光（`scene.remove(ctx.defaultLights)` + 加自己的）—— 那是设计
  //   · 改 `scene.background` / `scene.fog` / 相机的一切 —— 那是设计
  //   · 自己建 Texture / CanvasTexture / ShaderMaterial / 后处理用的 RenderTarget
  //   · `setTimeout`（一次性的延迟，不是画帧）
  //   · 任何数学、噪声函数、自己实现的数据结构
  //   · `Math.random`（粒子诞生位置、初速度、相位都该随机 —— 见 F 那条）
  //   ⟹ 判据：**骨架是"在有限预算里稳定产出"的手段，不是审美的笼子。**
  //     它锁的是"会坏"（渲染循环 / 音频算法 / 错误上报），放的是"长什么样"。

  // ── C2.⚠️⚠️⚠️ **删了灯还用需要光照的材质 = 必然全黑**（0.9.147）
  //
  // 用户 2026-08-03 实测："这次的效果看起来一片黑"，而读数是
  //   场景 **1 个对象**、高亮 **0%**、三带 12/9/6。
  // ⚠️ "1 个对象"是关键：骨架默认往 scene 里放了一个 `defaultLights` Group，
  //   所以只有 1 个对象意味着**模型删掉了默认灯光**（或者它一个都没加）。
  //   而 `MeshStandardMaterial` / `MeshPhongMaterial` / `MeshLambertMaterial`
  //   **没有光就是纯黑** —— 那不是"暗"，是字面的 (0,0,0)。
  //
  // ⚠️⚠️ 而这个组合**三轮都没被修掉**：回喂里只有"太暗了"这种现象描述，
  //   模型改的是颜色的 L 值 —— 而那改不动"没有光"这个根因。
  //   ⟹ 判据：**回喂要给根因，不是给现象。** "把亮度提上去"和
  //     "你删了灯又用了需要光的材质"是两条完全不同的指令。
  //
  // ⚠️ 我们**鼓励**模型换整套打光（0.9.140 特意放开的，见 MODULES.md ⑤b）——
  //   所以这条不是"不许删灯"，是"删了要么自己加，要么换成自发光材质"。
  const removesLights = /scene\.remove\s*\(\s*(ctx\.)?defaultLights/.test(src);
  const addsOwnLight = /new\s+THREE\.(Ambient|Directional|Point|Spot|Hemisphere|RectArea)Light/
    .test(src);
  const needsLight = /new\s+THREE\.Mesh(Standard|Phong|Lambert|Physical|Toon)Material/
    .test(src);
  if (removesLights && needsLight && !addsOwnLight) {
    add('C2-删了灯还用需光材质',
      '你删掉了 ctx.defaultLights，又用了需要光照的材质'
      + '（MeshStandard/Phong/Lambert/Physical/ToonMaterial），而没有加任何灯'
      + ' ⟹ **画面会是纯黑的**（那些材质没有光就是字面的 (0,0,0)，不是"暗"）。'
      + '\n两条路选一条：'
      + '\n① 换成 MeshBasicMaterial（自发光、扁平霓虹感，不需要灯）'
      + '\n② 保留 defaultLights，或者自己加灯'
      + '（比如 new THREE.AmbientLight(0xffffff, 0.4) + 一个 DirectionalLight）');
  }

  // ── C3.⚠️ 场景里必须真的加了东西
  //   ⚠️ 用户那次"场景 1 个对象" —— 而 1 就是骨架自己那个 defaultLights Group
  //     ⟹ 模型什么都没加，或者加了但没 add 进 scene。
  //   ⚠️ 判据：**`add(...)` 这个动作要能被看到** —— 建了 Mesh 不 add 进 scene
  //     是这类代码最常见的漏，而它的症状是"什么都不显示"且**不报错**。
  if (/new\s+THREE\.(Instanced)?Mesh\s*\(/.test(src)
      && !/(ctx\.)?scene\.add\s*\(/.test(src)
      && !/\.add\s*\(/.test(src)) {
    add('C3-没加进场景',
      '建了 Mesh 但看不到 scene.add(...) ⟹ 它不在场景里，画面上什么都没有'
      + '（而这个不报错）。建好之后要 ctx.scene.add(那个 mesh)');
  }

  // ── D.⚠️ InstancedMesh 用了就必须置 needsUpdate
  //   （不置的话画面完全静止而**不报错** —— 这个坑在骨架注释里也写着）
  if (/InstancedMesh/.test(src)) {
    if (/setMatrixAt/.test(src) && !/instanceMatrix\.needsUpdate\s*=\s*true/.test(src)) {
      add('D-忘了 needsUpdate',
        '改了 instance matrix 但没置 `instanceMatrix.needsUpdate = true`'
        + ' ⟹ GPU 上还是上一帧的数据，画面完全静止而不报错');
    }
    if (/setColorAt/.test(src) && !/instanceColor[\s\S]{0,80}needsUpdate/.test(src)) {
      add('D-忘了颜色 needsUpdate',
        '用了 setColorAt 但没置 instanceColor.needsUpdate ⟹ 颜色不会变');
    }
  }

  // ── E.⚠️ 空闲状态：没音乐时也要动
  //   ⚠️ 只能弱判 —— 找"和 ctx.t 有关的运动"。查不到就提醒，不算硬错误。
  if (!/ctx\.t\b|\bt\s*\*|Math\.(sin|cos)\s*\(\s*(ctx\.)?t/.test(src)) {
    add('E-没有空闲动画',
      '看不到任何和时间（ctx.t）相关的运动 ⟹ 没放音乐时画面会完全静止，'
      + '而用户会以为壁纸坏了');
  }

  // ── F.⚠️⚠️⚠️ 每帧 Math.random 决定位置 = 雪花噪点
  //
  // ⚠️⚠️ **这条我第一版写错了，而它连报三轮、模型改不掉**（实测 2026-08-02）：
  //   模型写的是**粒子诞生**（`particles.push({ vx: Math.cos(a)*s, vy: 0.5+Math.random()*1.5 })`）
  //   —— 那正是我自己那条规则说"诞生时随机是对的"的情况，
  //   而我的正则只看"frame() 里有没有 Math.random 挨着 x/y/z"，分不出
  //   "诞生时随机一次"和"每帧重新随机"。
  //   ⟹ 症状：闸门 3 轮报同一条、模型每轮都改一遍别的地方，白烧 60 秒。
  //   ⚠️ 判据：**一条闸门如果模型改不掉，先怀疑闸门错了** ——
  //     它连报三轮而产物在变，那说明它指的不是模型能改的东西。
  //
  // ⟹ 收窄到真正坏的那个形状：**直接给已有对象的坐标赋随机值**
  //   （`p.x = Math.random()` / `obj.position.x = Math.random()`），
  //   而 `push({...Math.random()...})`（诞生）和 `vx: Math.random()`（初速度）都放过。
  //   ⚠️ 宁可漏报也不误报：误报会让模型去改一段本来正确的代码（而实测它改不动，
  //     只会白烧三轮）。
  const frameAt = src.search(/\bframe\s*[(:]/);
  if (frameAt > 0) {
    const frameBody = src.slice(frameAt, frameAt + 6000);
    // ⚠️ 只逮"赋值给某个已存在对象的位置分量"
    const bad = /\.(position\.)?[xyz]\s*=\s*[^;\n]{0,30}Math\.random/.test(frameBody)
      || /\.position\.set\s*\([^)]{0,60}Math\.random/.test(frameBody);
    if (bad) {
      add('F-每帧随机位置',
        'frame() 里把已有元素的位置**赋成**随机值 ⟹ 它每帧跳到别处，'
        + '看起来是雪花噪点而不是运动。'
        + '\n⚠️ 位置要由索引+时间决定，或者存进数组各自积分'
        + '（而"新粒子诞生时随机一次"是对的，那不算）');
    }
  }

  return problems;
}

// ---------------------------------------------------------------------------
// ② 真跑闸门的判定（只判数据，不开窗口）
// ---------------------------------------------------------------------------
//
// ⚠️⚠️ 开窗口那部分在 main.js（要 Electron）。这里只做**判定** ——
//   那样它就能在云端被测到，而"判定逻辑错了"和"窗口没起来"是两类问题。
//
// ⚠️ 输入：{ ready, errors: [...], frames, ms, sampledPixels: {black, total} }
function judgeRuntime(probe) {
  const p = probe || {};
  const problems = [];
  const add = (id, detail) => problems.push({ id, detail });

  // ⚠️ JS 报错最优先 —— 它是白屏的直接原因，而且报错原文能直接回喂。
  for (const err of (p.errors || []).slice(0, 6)) {
    add('R-运行时报错', `页面里报错：${err}`);
  }

  if (p.ready === false) {
    add('R-没就绪', '页面没调 window.wallpaperReady() —— 要么它没调，'
      + '要么脚本在调到那一行之前就挂了（看上面的报错）');
  }

  // ⚠️ fps：分母是真实耗时，不是我假设的 3 秒 —— 窗口起得慢的话会把 fps 算低。
  if (Number.isFinite(p.frames) && Number.isFinite(p.ms) && p.ms > 500) {
    const fps = p.frames / (p.ms / 1000);
    // ⚠️ 阈值 8 而不是 20：**限帧到 40 的壁纸在离屏窗口里本来就慢**
    //   （不可见窗口会被 Chromium 降频）⟹ 20 那个阈值会把好产物砍掉。
    //   8 只用来逮"根本没在画"。
    if (fps < 8) {
      add('R-几乎不动', `${p.ms}ms 里只画了 ${p.frames} 帧（约 ${fps.toFixed(1)}fps）`
        + ' —— 要么动画循环没起来，要么每帧太重');
    }
  } else if (p.frames === 0) {
    add('R-一帧没画', '一帧都没画出来 —— requestAnimationFrame 循环没跑起来');
  }

  // ⚠️ 纯黑判定：**这是"跑起来了但什么都看不见"的唯一自动判据**，
  //   而那种失败在日志里完全干净（没报错、fps 正常）。
  const px = p.sampledPixels;
  if (px && px.total > 0) {
    const ratio = px.black / px.total;
    // ⚠️ 0.995 而不是 1.0 —— 有的壁纸主体很暗但有几个亮点，
    //   而"几乎全黑"和"全黑"在观感上是同一件事。
    if (ratio > 0.995) {
      add('R-画面全黑', `采样的 ${px.total} 个像素里 ${px.black} 个是近黑色`
        + '（画面基本什么都看不见）—— 常见原因：坐标算错画到屏幕外、'
        + '颜色 alpha 是 0、或者 shader 编译失败');
    }
  }

  return problems;
}

// ═══════════════════════════════════════════════════════════════════════════
//  ⚠️⚠️⚠️ **构图判定 —— "好不好看"里能机器判的那部分**（0.9.145）
// ═══════════════════════════════════════════════════════════════════════════
//
// 用户 2026-08-03：「出来的效果很抽象，不好看」+「先通过提示词来复刻出我这张
//   壁纸的效果，然后实现以后我们再把它泛化」
//
// ⚠️⚠️ "好不好看"整体上只有人能判（那是 MODULES.md 里第三层闸门的意思，不变）。
//   ⚠️ 但**其中有一部分是可测的** —— 我把参考壁纸的 `preview.gif` 200 帧
//     逐帧量化之后，"抽象不好看"和"有审美"在这几个数上差别极大：
//
//              参考壁纸（好看）      模型第一版（用户说抽象）
//     近黑占比    17-45%              近黑 297/400 = 74%（第一次）
//     高亮占比    4-21%
//     上 1/3     恒定 ~25（全暗）
//     中 1/3     58 → 110（主体）
//     饱和度中位  0.30-0.34           模型默认给高饱和霓虹
//
// ⟹ 判据：**把"审美"里能量化的那部分变成闸门，剩下的留给人。**
//   而这一层的价值不在"拦住丑的"，在**回喂**：模型拿到
//   "你的近黑只有 5%（该 20-45%）⟹ 元素铺满了整个画面" 这种具体数字，
//   下一轮能真的改；而"不好看"它改不了。
//
// ⚠️ 这些是**软判定**：它们进 problems（会触发下一轮修），但**不阻止落盘** ——
//   构图是审美，判错了不该让用户拿不到壁纸。
//   ⚠️ 判据：**硬闸门拦"必然出事"，软判定给"可能不好看"的具体数字。**
// ⚠️⚠️⚠️ **这一层测不到什么 —— 别以为它能挡住"不好看"**
//
// 我拿用户那次"很抽象不好看"的真实探针数据跑了一遍这个判定器：
//   近黑 74%、三带 20/70/40、饱和度中位 0.55 ⟹ **零问题**。
//
// ⚠️ 那不是判定器坏了，是**那张的问题不在像素分布上**：
//   · 256 根柱子（该上千个）⟹ "看得清每一根"的图表感
//   · 只有"整体膨胀 + 相机公转"两种动态（该三组独立的）
//   · 俯视 (0,14,18)（该压低到地平线附近）
//   这三条**都在提示词里说了**，但它们从 32×32 的截图上测不出来 ——
//   俯视和低视角在"三带亮度"上可以完全一样。
//
// ⟹ 判据：**说清一层闸门测不到什么，比夸大它测得到什么重要。**
//   这一层只能逮住四种"明显错的"（铺满 / 全亮 / 全平 / 荧光色），
//   而"元素太少""动态太单一""视角不对"要靠**看代码**（下面 inspectDesign）
//   和**用户的眼睛**。
//
// ⚠️ 而它仍然值得存在：那四种一旦发生，回喂的数字能让模型真的改。

function judgeComposition(probe) {
  const p = probe || {};
  const px = p.sampledPixels;
  const problems = [];
  if (!px || !px.total || !Array.isArray(px.bands)) return problems;
  const add = (id, detail) => problems.push({ id, detail });

  const blackRatio = px.black / px.total;
  const brightRatio = (px.bright || 0) / px.total;
  const [top, mid, bottom] = px.bands;

  // ── ① 画面要有大片"空"（参考：20-45%）
  //   ⚠️ 只判**下界** —— 上界那头由 R-画面全黑 管（0.995），
  //     而 45% 到 99% 之间是"很暗但有主体"，那可能是有意的（夜色风格）。
  if (blackRatio < 0.08) {
    add('C-铺满了', `近黑区域只占 ${(blackRatio * 100).toFixed(0)}%（参考壁纸是 20-45%）`
      + ' ⟹ 元素铺满了整个画面，观感是一片噪点。'
      + '收缩元素的分布范围、或者让远处的元素没入底色（雾/按距离降亮度）');
  }

  // ── ② 亮的地方要是少数（参考：4-21%）
  if (brightRatio > 0.45) {
    add('C-太亮了', `高亮区域占 ${(brightRatio * 100).toFixed(0)}%（参考壁纸是 4-21%）`
      + ' ⟹ 整个画面一样亮就没有"亮点"了。'
      + '把大部分元素压暗，只让中心一小片亮');
  }

  // ── ②b ⚠️⚠️⚠️ **而"太暗"也要判** —— 我第一版只判了上界。
  //   用户 2026-08-03 实测："这次的效果看起来一片黑"，而读数是
  //   **高亮 0%、三带 12/9/6** ⟹ 六条构图判定只报了一条（主体不在中间），
  //   "一片黑"这个最显眼的问题**一条都没逮住**。
  //   ⚠️ 而 `R-画面全黑`（近黑 > 99.5%）也没报 —— 因为它有 50% 的近黑，
  //     不是"全黑"，是**"有东西但全都很暗"**。那是两种不同的失败。
  //   ⟹ 判据：**一个区间要判两头。** 我判了"铺满/太亮"却漏了"太空/太暗"，
  //     而后者恰恰是这次发生的。
  if (brightRatio < 0.01) {
    add('C-太暗了', `高亮区域只占 ${(brightRatio * 100).toFixed(1)}%（参考壁纸是 4-21%）`
      + ' ⟹ 画面上没有任何"亮"的地方，看起来就是一片黑。'
      + '把中心那一小片的亮度提上去（L 0.7 以上），让它成为视觉焦点');
  }
  // ⚠️ 整体亮度太低（参考：中带 58-110）
  if (Number.isFinite(mid) && mid < 20 && blackRatio < 0.9) {
    add('C-整体太暗', `主体那一带（中 1/3）平均亮度只有 ${mid.toFixed(0)}`
      + '（参考壁纸是 58-110）⟹ 元素在那儿但太暗，看不出形状。'
      + '把整体亮度提上去：材质颜色的 L 至少 0.4，中心那片 0.7 以上');
  }

  // ── ③ 上 1/3 该是暗的（参考：恒定 ~25）
  //   ⚠️ 阈值 70 而不是 25 —— 25 是那一张的值，别把它当所有壁纸的标准。
  //     70 只用来逮"主体伸满了整个画面高度"。
  if (Number.isFinite(top) && top > 70) {
    add('C-上方太亮', `画面上 1/3 的平均亮度是 ${top.toFixed(0)}（参考壁纸约 25）`
      + ' ⟹ 主体伸进了上方，画面没有"天空"。'
      + '把相机压低、或者让元素只占画面中间那一横条');
  }

  // ── ④ 主体该在中间那一横条（参考：中 > 上）
  if (Number.isFinite(top) && Number.isFinite(mid) && mid < top * 1.15) {
    add('C-主体不在中间', `中 1/3 亮度 ${mid.toFixed(0)} 没比上 1/3（${top.toFixed(0)}）亮`
      + ' ⟹ 主体没落在画面中间那一横条上（参考壁纸是中间最亮）。'
      + '调相机的 lookAt 高度');
  }

  // ── ⑤⚠️⚠️ 饱和度（参考：中位 0.30-0.34，激烈时才到 0.65）
  //   这条最容易丢，而它是"一眼就俗"的主因。
  if (Number.isFinite(px.satMedian) && px.satMedian > 0.72) {
    add('C-颜色太艳', `主体饱和度中位 ${px.satMedian.toFixed(2)}（参考壁纸是 0.30-0.34，`
      + '最激烈时才 0.65）⟹ 高饱和霓虹色看起来很廉价。'
      + '把 S 降到 0.3-0.5，让亮度差而不是饱和度差来分层');
  }

  // ── ⑥ 明暗要分层（三带都差不多 = 平的）
  if ([top, mid, bottom].every(Number.isFinite)) {
    const mx = Math.max(top, mid, bottom);
    const mn = Math.min(top, mid, bottom);
    // ⚠️⚠️ 阈值从 25 降到 8 —— 用户那次三带是 **12/9/6**，`mx=12 < 25`
    //   ⟹ 这条整个跳过了，而那张确实"没有明暗层次"（也确实一片黑）。
    //   ⚠️ 而 8 以下真的是全黑（`R-画面全黑` 管），那时候不该报"没层次"
    //     （那是废话）。
    if (mx > 8 && mx - mn < mx * 0.25) {
      add('C-没有明暗层次',
        `上/中/下三带亮度是 ${top.toFixed(0)}/${mid.toFixed(0)}/${bottom.toFixed(0)}`
        + '，几乎一样 ⟹ 画面是平的。'
        + '让亮度按"到画面中心的距离"衰减（中心偏白、外围没入底色）');
    }
  }

  return problems;
}

// ═══════════════════════════════════════════════════════════════════════════
//  ⚠️⚠️ **设计检查 —— 从代码里读"元素多不多/视角高不高/动态几种"**（0.9.145）
// ═══════════════════════════════════════════════════════════════════════════
//
// 上面 judgeComposition 从像素测不到的那三条，恰好**能从代码里读**：
//   · 元素个数 ⟹ InstancedMesh 的第三个参数、或者循环上界
//   · 相机高度 ⟹ camera.position.set(x, y, z) 的 y 和 z 的比
//   · 动态种类 ⟹ 有没有"事件触发"的那种（数组 push + 每帧衰减 + filter）
//
// ⚠️ 这些是**软判定**（进 problems 触发下一轮修，但不阻止落盘）——
//   它们判的是审美取向，而误报会让模型去改一段本来合理的代码。
//   ⚠️ 判据：**能从代码读的就别猜像素。** 而两层都软，因为两层都可能误判。
function inspectDesign(code) {
  const src = String(code || '');
  const problems = [];
  const add = (id, detail) => problems.push({ id, detail });

  // ── ① 元素个数：参考壁纸是几千个小点，而"看得清每一根"就是图表感
  //   ⚠️ 只在**用了 InstancedMesh** 时判 —— 没用的话 D 闸门那边会提，
  //     而这里判不出个数（几十个 Mesh 也可能是对的，比如一个大球体）。
  const inst = src.match(/new\s+THREE\.InstancedMesh\s*\([^,]+,[^,]+,\s*([^)]+)\)/);
  if (inst) {
    // ⚠️ 第三个参数可能是表达式（`cols * rows`、`N * N`）⟹ 找得到数字就估，
    //   找不到就不判（**宁可不判也别误判** —— 见 F 闸门那次误报三轮的教训）。
    const nums = inst[1].match(/\d+/g);
    if (nums && nums.length) {
      const est = nums.map(Number).reduce((a, b) => a * b, 1);
      if (est > 0 && est < 800) {
        add('C-元素太少',
          `InstancedMesh 只有约 ${est} 个实例（参考壁纸是几千个）`
          + ' ⟹ 元素少而大时看得清每一个，观感是图表不是风景。'
          + '把个数提到 2000-8000，单个尺寸相应缩小');
      }
    }
  }

  // ── ②⚠️ 相机高度：俯视 vs 贴近地平线
  //   参考壁纸：场景半径 ~20，相机 (0, 4, 22) ⟹ y/z ≈ 0.18
  //   模型第一版：(0, 14, 18) ⟹ y/z ≈ 0.78（俯视）
  const cam = src.match(/camera\.position\.set\s*\(\s*([-\d.]+)\s*,\s*([-\d.]+)\s*,\s*([-\d.]+)\s*\)/);
  if (cam) {
    const y = Math.abs(Number(cam[2]));
    const z = Math.abs(Number(cam[3]));
    // ⚠️ z 太小时不判（那可能是刻意的正视/俯视构图，比如从上往下看漩涡）
    // ⚠️⚠️⚠️ 阈值从 0.55 放到 0.75 —— 因为 **0.55 把我们自己那份示例场景
    //   判红了**（`scene.example.js` 是 (0, 15, 26)，y/z = 0.58），
    //   而那份是用户在真机上跑过、说"能看"的。
    //   ⟹ 判据：**一条闸门把已知可接受的产物判红，就是阈值错了。**
    //     （这个项目为"闸门误报正确写法"栽过 —— F 闸门连三轮误报粒子诞生
    //      的 Math.random，模型每次都是对的。）
    //   ⚠️ 0.75 只逮"明显是俯视看全景"的（用户那版是 14/18 = 0.78）。
    if (z > 3 && y / z > 0.75) {
      add('C-视角太高',
        `相机在 (…, ${cam[2]}, ${cam[3]})，高度/距离 = ${(y / z).toFixed(2)}`
        + '（参考壁纸约 0.18）⟹ 那是俯视看全景，观感像几何演示。'
        + '把相机压低到贴近地平线（高度不超过距离的 1/4）');
    }
  }

  // ── ⑧⚠️⚠️ **空闲动画写在 else 里 ⟹ 有音乐时它就没了**（0.9.150，实测）
  //
  // 用户 2026-08-03 那张（08030141）的高度算式：
  //   `if (hasAudio) { target = 频谱... } else { target = 行波... }`
  //   ⟹ 有音乐时**行波那一层整个消失**，而那正是"整片都在起伏"的来源
  //   ⟹ 剩下的只有"每个点各自跟自己那段频谱" = 噪点感，不是地形。
  //
  // ⚠️ 用户的原话是「动态的部分太少了」—— 而它的动态**在有音乐时比静音时更少**，
  //   那是反直觉的，也正是二选一造成的。
  //   ⟹ 判据：**基础动态要一直在，音频是叠加上去的。**
  //     二选一会让"有音乐"这个本该更热闹的状态反而更单调。
  //
  // ⚠️ 判定：高度/位置的算式里出现 `if (hasAudio) ... else ...` 这种二分，
  //   而 `else` 那支里有 `Math.sin(t` 这类时间驱动的东西（那就是空闲动画）。
  const idleInElse = /if\s*\(\s*!?\s*(hasAudio|audio\.everGot|playing)[^)]*\)\s*\{[\s\S]{0,400}?\}\s*else\s*\{[\s\S]{0,300}?Math\.(sin|cos)\s*\(\s*[^)]*\bt\b/
    .test(src);
  if (idleInElse) {
    add('C-空闲动画二选一',
      '空闲动画（跟时间走的那个波）写在 `else` 分支里 ⟹ **有音乐时它整个消失**。'
      + '\n⚠️ 那会让"有音乐"这个本该更热闹的状态反而更单调 ——'
      + '因为"整片都在起伏"那一层被抽掉了，剩下的只有各点各自跟频谱 = 噪点感。'
      + '\n⟹ 改成**叠加**：'
      + '\n  `let h = 基线 + 空闲波(t, dist);        // 一直在`'
      + '\n  `if (hasAudio) h += 频段能量 * 强度;    // 音频叠上去`'
      + '\n⚠️ 而空闲波的幅度可以在有音乐时**调小**（比如 ×0.4），但不能变成 0。');
  }

  // ── ⑦⚠️⚠️⚠️ **没用播放器给的 DP 曲线**（0.9.150）
  //
  // 用户 2026-08-03 那张里 `DP.` 出现 **0 次** —— 我注入了那段代码、
  // 提示词里也写了"原样抄进去然后调它"，而它**完全忽略了**，自己推了一套。
  // ⟹ 而它自己推的那套正是死因（三次衰减 / 雾密度 0.12）。
  //
  // ⚠️ 判据：**"我给了它"不等于"它用了"** —— 提示词里的要求是软的。
  //   ⟹ 这条要拦，而且报错里要说清"为什么必须用"（不是"我要求你用"）。
  const usesDP = /\bDP\.(lum|sat|hue|fogDensity)\s*\(/.test(src);
  const setsColor = /setHSL|setRGB|\.color\s*=/.test(src);
  if (setsColor && !usesDP) {
    add('C-没用给的配色曲线',
      '你自己推了配色/亮度公式，而没用播放器提供的 `DP.lum` / `DP.sat` / `DP.hue`。'
      + '\n⚠️ 那三个函数的数字是从目标壁纸**逐帧量化**出来的（近黑占比、'
      + '三分带亮度、饱和度中位），而自己推公式实测栽过两次：'
      + '\n  · 一次写成三次衰减（`lum` 被 `*=` 两遍）⟹ 外圈纯黑、高亮占比 0%'
      + '\n  · 一次雾密度填 0.12 ⟹ 整片被乘掉 91%，而调 L 完全无效'
      + '\n⟹ 把提示词里那段 `DP` 原样抄进 scene.js 顶部，然后：'
      + '\n  `color.setHSL(DP.hue(d, loud) / 360, DP.sat(d, loud), DP.lum(d, energy))`'
      + '\n  `ctx.scene.fog = new THREE.FogExp2(DP.bg, DP.fogDensity(相机距离, 场景半径))`'
      + '\n⚠️ 你**可以**在 DP 之外加自己的东西（比如给亮核额外加亮），'
      + '但基线要用 DP —— 那样"整片被压暗"这类问题不会再发生。');
  }

  // ── ⑥⚠️⚠️⚠️ **雾太浓 = 整片被乘掉**（0.9.150，实测死因）
  //
  // 用户 2026-08-03 那张（wallpaper-08030141）：
  //   `new THREE.FogExp2(BG, 0.12)` + 相机在 (0, 3.2, 12)
  //   ⟹ FogExp2 的透光率 = exp(-(density*distance)²) = exp(-(0.12*13)²) ≈ **0.086**
  //   ⟹ 所有元素被乘掉 **91%**。模型把 L 给到 0.85，乘完变 0.073。
  //     那就是读数"三带 8/10/10、高亮 0%"的来源。
  //
  // ⚠️⚠️ 而模型**三轮都在调 L**（诊断每轮都很到位，但没有一轮怀疑到雾）——
  //   因为雾在 `setHSL` **之后**作用，它看不见那个乘法。
  //   ⟹ 判据：**当一个参数的效果隔着好几层才显现，模型不可能靠试错找到它。**
  //     那种参数要用代码算 + 在观测里把它的影响直接摆出来。
  const fogExp = src.match(/new\s+THREE\.FogExp2\s*\([^,]+,\s*([\d.]+)\s*\)/);
  const camZ = src.match(/camera\.position\.set\s*\([^,]+,[^,]+,\s*([-\d.]+)\s*\)/);
  if (fogExp) {
    const den = Number(fogExp[1]);
    // ⚠️ 相机距离拿不到时用 20（那是我们建议的场景尺度）
    const dist = camZ ? Math.abs(Number(camZ[1])) : 20;
    const trans = Math.exp(-Math.pow(den * dist, 2));
    // ⚠️ 相机那个距离处透光率低于 45% 就是"整片被压暗"
    //   （参考：算出来的合理密度在相机处是 ~68%）
    if (Number.isFinite(trans) && trans < 0.45) {
      add('C-雾太浓',
        `FogExp2 的密度是 ${den}，而相机在 ${dist}m ⟹ 透光率只有 `
        + `**${(trans * 100).toFixed(0)}%**（exp(-(${den}×${dist})²)）`
        + ` ⟹ 所有元素的亮度被乘掉 ${(100 - trans * 100).toFixed(0)}%。`
        + '\n⚠️ 雾在 setHSL **之后**作用 ⟹ 你把 L 调到 0.9 也只剩 '
        + `${(0.9 * trans).toFixed(2)}，调 L 是**没用的**。`
        + '\n⟹ 用 `DP.fogDensity(相机距离, 场景半径)` 算那个数（别自己填）。'
        + '雾该做的是"让最外圈没入底色"，不是"让整片变暗"。');
    }
  }
  // ⚠️ 线性 Fog 同理 —— near/far 设得太近会把主体整个吃掉
  const fogLin = src.match(/new\s+THREE\.Fog\s*\([^,]+,\s*([\d.]+)\s*,\s*([\d.]+)\s*\)/);
  if (fogLin && camZ) {
    const near = Number(fogLin[1]);
    const far = Number(fogLin[2]);
    const dist = Math.abs(Number(camZ[1]));
    // ⚠️ 相机距离已经超过 far ⟹ 主体完全在雾里（纯底色）
    if (Number.isFinite(far) && far > 0 && dist >= far) {
      add('C-雾太浓',
        `线性 Fog 的 far 是 ${far}，而相机在 ${dist}m ⟹ **主体完全在雾里**`
        + '（画出来就是纯底色）。⟹ far 至少要是相机距离 + 场景半径的 1.5 倍');
    } else if (Number.isFinite(near) && Number.isFinite(far) && dist > near
        && (dist - near) / Math.max(1, far - near) > 0.6) {
      add('C-雾太浓',
        `线性 Fog 的 near=${near} far=${far}，相机在 ${dist}m`
        + ` ⟹ 主体已经被雾吃掉 ${(100 * (dist - near) / (far - near)).toFixed(0)}%。`
        + 'near 要放到相机距离之后（让近处清晰），far 放到场景边缘之外');
    }
  }

  // ── ④⚠️⚠️⚠️ **亮度连乘 = 外圈纯黑**（0.9.148，实测）
  //
  // 用户 2026-08-03 那张（wallpaper-08030114）的代码里：
  //   lum = 0.4 * (1 - dd) * (1 - dd);   ← 已经是平方衰减
  //   ...
  //   lum *= (0.1 + 0.9 * (1 - dd));     ← **又乘了一次**，外圈到 0.1
  // ⟹ 结果：高亮像素占 **0.0%**，画面只有正中心一个亮尖。
  // ⚠️ 而模型自己的注释里写着"根因分析：上一版用 setHSL 时把 lum 一路乘小" ——
  //   它**诊断对了，改的时候又犯了一次**。
  //   ⟹ 判据：**模型能说对根因不等于它能改对** ⟹ 这种要用代码拦。
  //
  // ⚠️ 判定：同一个变量在**同一个作用域里被 `*=` 两次以上**，或者
  //   一次赋值里出现两个以上 `(1 - x)` 因子。
  //   ⚠️ 而这条**只针对亮度**（变量名含 lum/bright/light/l 或 setHSL 的第三个参数）
  //     —— 位置/缩放连乘是正常的。
  const lumLines = src.split('\n').filter((line) => /\b(lum|bright|lightness)\b/i.test(line));
  const mulAssign = lumLines.filter((line) => /\b(lum|bright|lightness)\w*\s*\*=/i.test(line));
  if (mulAssign.length >= 2) {
    add('C-亮度连乘',
      `亮度被 \`*=\` 了 ${mulAssign.length} 次 ⟹ 那是多次衰减，外圈会变成**纯黑**`
      + '而不是"暗"（实测过：外圈乘到 0.1，画面高亮占比 0%）。'
      + '\n⟹ 衰减只做一次：L = 0.85 - 0.6 * d（d 是 0..1 的归一化距离）');
  }
  // ⚠️ 一次赋值里两个 (1-x) 因子也是平方衰减
  const squared = lumLines.some((line) => (line.match(/\(\s*1\s*-\s*\w+\s*\)/g) || []).length >= 2);
  if (squared && mulAssign.length >= 1) {
    add('C-亮度连乘',
      '亮度先做了平方衰减（两个 (1 - x) 因子）又 `*=` 了一次'
      + ' ⟹ 三次衰减，外圈必然纯黑。衰减只做一次');
  }

  // ── ⑤⚠️⚠️ **高度直接由"到中心的距离"决定 = 一个圆锥**（0.9.148，实测）
  //
  // 用户 2026-08-03 那张就是这样：中间一个尖、四周越来越低
  // ⟹ 观感是"一个几何体"，而参考壁纸是"一整片起伏的地形"。
  // ⚠️ 到中心的距离该只用来**选频段**（低频在中间、高频在外圈），
  //   **不直接决定高度** —— 那两件事要分开。
  //   ⟹ 高度要 = 该点自己的频段能量 + 跟**位置**有关的噪声/波函数。
  //
  // ⚠️ 而这条只能弱判（判不出"有没有叠位置噪声"这件事的所有写法）
  //   ⟹ 找"高度赋值里出现了距离变量、而整个 frame 里没有跟 x/z 有关的三角函数"。
  //
  // ⚠️⚠️⚠️ 我第一版判"高度里有距离变量 && 没有 Math.sin(x)"——
  //   而用户那张**两个条件都满足**（它有 `Math.sin(x*0.3)*Math.cos(z*0.3)`）
  //   ⟹ 没逮住。看代码才发现真正的形状：
  //     那个位置噪声**只在"没音频"那条分支里**；有音频时高度是
  //     `target = 0.3 + e * 3.5 + audio.bass * 0.9 + core * 2.6`
  //     ⟹ `core`（到中心的距离）那一项**系数 2.6，和频谱的 3.5 同量级**
  //       ⟹ 中心那个尖是**硬加上去的**，不管音乐怎么放它都在。
  //   ⟹ 判据：**判"距离项的权重"，不是判"有没有位置噪声"。**
  //     （而这条只能弱判 —— 系数是表达式时算不出来，那时不判。）
  const lines = src.split('\n');
  // 找"高度目标"那一行的赋值（target/h/height = ...）
  const heightLines = lines.filter((line) =>
    /\b(target|h|height|hh)\s*=\s*[^=]/.test(line)
    && /\b(core|dist|distFromCenter|dc|rNorm)\b/.test(line));
  for (const line of heightLines) {
    // ⚠️ 找 `core * 数字` 这种项，系数够大就是"硬加了个圆锥"
    const m = line.match(/\b(core|dist|distFromCenter|dc|rNorm)\s*\*\s*([\d.]+)/);
    if (!m) continue;
    const coef = Number(m[2]);
    // ⚠️ 和同一行里最大的那个系数比 —— 距离项和频谱项同量级就说明它在塑形
    const allCoefs = [...line.matchAll(/\*\s*([\d.]+)/g)].map((x) => Number(x[1]));
    const mx = Math.max(...allCoefs);
    if (Number.isFinite(coef) && coef >= mx * 0.5 && coef >= 1) {
      add('C-像个圆锥',
        `高度里有一项 \`${m[1]} * ${coef}\`（${m[1]} 是到中心的距离），`
        + `而它和这一行最大的系数 ${mx} 同量级`
        + ' ⟹ 那等于**硬加了一个圆锥**：中间一个尖、四周越来越低，'
        + '不管音乐怎么放它都在。观感是"一个几何体"而不是风景。'
        + '\n⟹ 到中心的距离**只用来选频段**（低频在中间、高频在外圈）；'
        + '高度 = 该点的频段能量 + 一层跟**位置**有关的噪声'
        + '（`Math.sin(x*0.3) * Math.cos(z*0.4)` 这类，而且**有音频时也要有**），'
        + '那样整片都在起伏、高低随位置分布，才像地形。');
      break;
    }
  }

  // ── ③⚠️⚠️ 动态种类：有没有"事件触发 + 自己衰减消失"的那种
  //   ⚠️ 那是参考壁纸最显眼的机制（bass 砸下来放一圈波纹出去），
  //     而只有"整体缩放/旋转"的话观感就是用户那句"自己在那转圈"。
  //   ⚠️ 判据：**找那个模式的三个动作**（push 进数组 / 每帧推进 / 到期 filter 掉），
  //     而不是找关键词 —— 变量叫 ripples 还是 pulses 还是 waves 都行。
  const hasSpawn = /\.push\s*\(\s*\{/.test(src);
  const hasDecay = /(life|age|alpha|r|radius|t)\s*[-+]=\s*[^;]*\bdt\b/.test(src)
    || /\*=\s*0\.9/.test(src);
  const hasReap = /\.filter\s*\(/.test(src);
  if (!(hasSpawn && hasDecay && hasReap)) {
    const missing = [
      !hasSpawn && '往数组里 push 新的（事件发生时）',
      !hasDecay && '每帧推进它（半径扩大/寿命减少）',
      !hasReap && '到期 filter 掉',
    ].filter(Boolean);
    add('C-只有一种动态',
      '看不到"事件触发 → 自己扩散 → 消失"这种动态'
      + `（缺：${missing.join('、')}）`
      + ' ⟹ 只有整体缩放/旋转的话观感就是"它在那转圈"。'
      + '加一个：bass 超阈值时往数组里 push 一个波纹 {x, z, r: 0, life: 1}，'
      + '每帧 r += dt * 速度、life -= dt * 衰减，life <= 0 时 filter 掉；'
      + '渲染时按元素到波纹的距离给它加高度/亮度');
  }

  return problems;
}

// ---------------------------------------------------------------------------
// project.json —— 让生成的壁纸变成这个播放器认的东西
// ---------------------------------------------------------------------------
//
// ⚠️ type 必须是 `Web`（WE 的类型名，首字母大写）—— 播放器按它选渲染路径。
// ⚠️ `audio.enabled` 不写的话音频通道不会打开（症状：接了音频回调但永远收不到数据）。
function buildProjectJson(title, description) {
  return {
    title: title || 'AI 生成壁纸',
    // ⚠️ 这个字段名是 `file`（不是 `main` / `index`），值是相对路径。
    file: 'index.html',
    type: 'Web',
    audio: { enabled: true },
    contentrating: 'Everyone',
    description: description || '',
    // ⚠️ 标记来源 —— 面板上要能分辨"这张是 AI 生成的"，
    //   而且出问题时能知道该去看生成记录。
    //   ⚠️ 这是我们自己加的字段，WE 不认识它（但它容忍未知字段）。
    // ⚠️⚠️ 0.9.146 **去掉了 gwRecipe**：配方那套（五维枚举 + 读历史避重）拆了 ——
    //   用户明确要"不做记忆系统，每次从零开始"，而留个空字段只会让
    //   下一个人以为它有用。⟹ 判据：**拆功能要连它的数据字段一起拆。**
    gwGenerated: true,
  };
}

// ---------------------------------------------------------------------------
// 目录名
// ---------------------------------------------------------------------------
//
// ⚠️ 用户的描述直接当目录名会炸：斜杠、冒号、换行、几百字。
//   ⚠️ 而 macOS 上 `/` 和 `:` 都不能进文件名（`:` 是历史上的路径分隔符，
//     Finder 里会显示成 `/`）。
function slugifyTitle(text, stamp) {
  const cleaned = String(text || '').replace(/\s+/g, ' ').trim()
    // eslint-disable-next-line no-control-regex
    .replace(/[/\\:*?"<>| -]/g, '')
    .slice(0, 24)
    .trim();
  const base = cleaned || 'AI壁纸';
  // ⚠️ 带时间戳 —— 同一句描述生成两次要是两个目录，
  //   否则第二次会覆盖第一次（而用户可能还想对比）。
  return stamp ? `${base}-${stamp}` : base;
}

module.exports = {
  buildPlanPrompt,
  buildImplementPrompt,
  readSkeletonSource,
  CONTRACT,
  CONSTRAINTS,
  SKELETON_API,
  buildObservation,
  measureCode,
  buildRepairPrompt,
  extractScene,
  extractDiagnosis,
  inspect,
  judgeRuntime,
  judgeComposition,
  inspectDesign,
  buildProjectJson,
  slugifyTitle,
};
